[workspace]
members = ["shared", "server", "client"]
resolver = "2"

[workspace.dependencies]
bevy = "0.18"
lightyear = { version = "0.26", default-features = false }
serde = { version = "1", features = ["derive"] }
